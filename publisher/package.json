{
  "name": "kafka-poc",
  "version": "1.0.0",
  "description": "Run the following command in the directory where your docker-compose.yml file is located:",
  "main": "consumer1.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "kafkajs": "^2.2.4"
  }
}
